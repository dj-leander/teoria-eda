#ifndef ALGORITHMS_N_REINES_H
#define ALGORITHMS_N_REINES_H

#include "utils.h"

void reines(int i, const int& n);

void reines(int n);

bool legal(int i, const int& n);

void escriu(VI& t, const int& n);

#endif //ALGORITHMS_N_REINES_H
